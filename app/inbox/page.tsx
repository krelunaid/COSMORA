import Image from 'next/image';
import Link from 'next/link';
import { Search } from 'lucide-react';
import { MobileNav, MobileShell } from '@/components/mobile-shell';

const chats = [['Ana (Spain)','Hola! Quiero encargar una espada…','2m','/category-artist.png'],['Lucas (Brazil)','Quando você pode enviar?','15m','/hd-category-figures.png'],['Yuki (Japan)','この衣装のサイズはありますか？','1h','/cosmora-hero.png'],['MangaVault','Your order has shipped.','3h','/hd-category-manga.png']];
export default function InboxPage() { return <MobileShell className="flex flex-col"><header className="flex h-16 items-center justify-between px-4"><h1 className="text-xl font-semibold">Inbox</h1><Search className="size-5" /></header><div className="flex-1 px-4"><div className="grid grid-cols-2 border-b border-white/10 text-center text-[10px]"><span className="border-b-2 border-pink-400 py-3 text-pink-300">Messages</span><span className="py-3 text-white/50">Orders</span></div><div className="divide-y divide-white/8">{chats.map(([name,message,time,image]) => <Link href="/inbox" key={name} className="flex items-center gap-3 py-4"><span className="relative size-12 overflow-hidden rounded-full"><Image src={image} alt="" fill sizes="48px" className="object-cover" /></span><div className="min-w-0 flex-1"><p className="text-xs font-medium">{name}</p><p className="mt-1 truncate text-[10px] text-white/45">{message}</p><p className="mt-1 text-[8px] text-violet-300">AI translation available</p></div><span className="text-[9px] text-white/35">{time}</span></Link>)}</div></div><MobileNav active="inbox" /></MobileShell>; }
